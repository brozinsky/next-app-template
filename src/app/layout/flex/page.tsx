"use client";
import WithWrapper from "@/components/HOC/WithWrapper";

const FlexPage = () => {
    return (
        <>
            <div className="grid w-full h-full grid-cols-2">FlexPage</div>
        </>
    );
};

export default WithWrapper(FlexPage);
